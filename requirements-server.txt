# ─── Launchway API Server dependencies ───────────────────────────────────────
# Install with: pip install -r requirements-server.txt
#
# The server also needs the CLI deps:
#   pip install -r requirements-cli.txt -r requirements-server.txt

# Web framework
Flask>=2.3.0
Flask-CORS>=4.0.0
waitress>=2.1.2

# Auth / security
PyJWT>=2.8.0
flask-talisman>=1.1.0

# Caching & queues
redis>=4.6.0
hiredis>=2.2.0
schedule>=1.2.0

# Cloud storage / backup
boto3>=1.28.0
botocore>=1.31.0

# Monitoring
sentry-sdk[flask]>=1.38.0
prometheus-client>=0.19.0

# Input validation
marshmallow>=3.20.0
bleach>=6.0.0
limits>=3.6.0
